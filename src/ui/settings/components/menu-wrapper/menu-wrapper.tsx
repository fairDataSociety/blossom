import { styled } from '@mui/system'

const MenuWrapper = styled('div')(() => ({
  minWidth: '350px',
  maxWidth: '350px',
  minHeight: '500px',
  maxHeight: '500px',
  height: '500px',
  padding: '20px',
  overflow: 'auto',
}))

export default MenuWrapper
