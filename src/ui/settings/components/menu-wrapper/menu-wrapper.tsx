import { styled } from '@mui/system'

const MenuWrapper = styled('div')(() => ({
  minWidth: '300px',
  minHeight: '500px',
  maxHeight: '500px',
  height: '500px',
  padding: '20px',
  overflow: 'auto',
}))

export default MenuWrapper
