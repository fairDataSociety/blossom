import React from 'react'
import { CircularProgress } from '@mui/material'

const FieldSpinner = () => <CircularProgress size="small" sx={{ width: '30px', margin: 'auto 10px' }} />

export default FieldSpinner
